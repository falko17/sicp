#|
> Are these processes iterative or recursive?

The first process is recursive (as it is not a tail call—it is used as the argument of another function).
The second process is iterative, as it is a tail call.
|#
